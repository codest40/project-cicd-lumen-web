services:
  - type: web
    name: project-cicd-lumen-web
    env: python
    plan: free
    buildCommand: |
      pip install --upgrade pip
      pip install -r requirements.txt
    startCommand: ./run_start.sh
    envVars:
      - key: DATABASE_URI
        value: DB_URL_EXTERNAL
